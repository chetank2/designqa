# Frontend Environment Configuration
# API URL for backend server
VITE_API_URL=http://localhost:3007

# WebSocket URL for real-time updates
VITE_WS_URL=ws://localhost:3007

# Server port (used by proxy configuration)
VITE_SERVER_PORT=3007

# Feature flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_AI_INSIGHTS=true
VITE_ENABLE_REAL_TIME=true

# Supabase Configuration (REQUIRED for SaaS mode)
# These variables MUST be set in frontend/.env for Vite to inject them at build time
# Vite only reads environment variables from frontend/.env, not from the root .env
# Run 'npm run sync:env' from the repo root to sync VITE_ vars from root .env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
